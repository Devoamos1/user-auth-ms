on:
  pull_request:
    types: [ opened, reopened, synchronize ]
jobs:
  call-pr-build-workflow:
    uses: theshareplatform/share-reusable-workflows/.github/workflows/pr-build.yaml@feature/BLOG-17-reusable-workflows
    with:
      java-version: '17'
      java-distribution: 'temurin'
